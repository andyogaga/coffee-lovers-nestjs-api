export enum GenericError {
  ERROR = 'Something went wrong, Please try again',
}

export enum CoffeeError {
  CANNOT_CREATE = 'Could not create coffee, Try again',
  NOT_FOUND = 'There is no coffee with the provided details',
  CANNOT_DELETE = 'Coffee details could not be deleted, Try again',
  CANNOT_UPDATE = 'Coffee details could not be updated, Try again',
}
